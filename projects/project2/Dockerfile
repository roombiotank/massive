# Dockerfile for project2
FROM php:8.1-fpm

# Copy project code
COPY . /var/www/project2

# Optional: install extensions
# RUN docker-php-ext-install mysqli pdo pdo_mysql

WORKDIR /var/www/project2
EXPOSE 9000
